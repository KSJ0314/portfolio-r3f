/**
 * 텍스처 한 장이 덮는 월드 크기(단위). 텍스처에 모눈 8칸이 들어 있으므로
 * 8이면 한 칸이 1유닛 — 캐릭터가 한 칸 남짓이 된다.
 */
export const PAPER_TILE_SIZE = 8

/** 낮에는 종이색을 그대로 쓴다. 밤에는 이 색을 곱해 어둡게 만든다. */
export const PAPER_DAY_TINT = '#ffffff'
export const PAPER_NIGHT_TINT = '#5d6690'
