/**
 * 펼친 패널의 폭(화면 폭 대비 %).
 *
 * 화면 크기를 따라가므로 넓은 모니터에서 좁아 보이거나 작은 창에서 화면을 다 덮지 않는다.
 * 마우스 없는 기기에서는 이 값을 쓰지 않고 화면을 가득 채운다.
 */
export const PANEL_WIDTH_VW = 40

/** 프로필 카드를 누르면 가는 스테이션. 카드에 담긴 것이 그 화면의 내용이다. */
export const INTRO_STATION_ID = 'about-intro'

/** 손잡이 크기. 패널 오른쪽 가장자리에 붙어 함께 미끄러진다. */
export const HANDLE_WIDTH = 26
export const HANDLE_HEIGHT = 84

/** 펼치고 접는 데 걸리는 시간(초). */
export const SLIDE_SECONDS = 0.28

/**
 * 층위.
 *
 * 좌상단 뒤로 가기(100)보다 **위**다 — 패널이 화면 높이를 다 쓰므로 그 버튼과 자리가 겹치는데,
 * 아래에 두면 버튼 글씨가 패널 위로 비어져 나온다.
 * 가로 회전 안내(110)와 장면 전환 덮개(200)보다는 아래다. 오가는 동안에는 가려지고 눌리지도 않는다.
 */
export const PANEL_Z = 105
