/**
 * 마우스 없는 기기를 목록 보기(`/list`)로 보낼지.
 *
 * 켜면 맵(`/`)에 들어선 순간 3D를 그리기 전에 목록 보기로 옮긴다.
 * 지금은 3D를 막는 대신 가로 화면만 요구하는 쪽으로 두어 꺼 놓았다.
 * 되살리려면 이 값만 `true`로 바꾼다.
 */
export const REDIRECT_MOBILE_TO_LIST = false

/**
 * 마우스 없는 기기에 들어오자마자 안내 화면을 띄울지.
 *
 * 목록 보기 자동 이동과 짝이라 함께 꺼 둔다.
 * 3D를 볼 수 있는데 목록으로 보여준다고 안내하면 화면과 어긋난다.
 * 꺼도 가로 회전 안내는 그대로 뜬다.
 */
export const SHOW_MOBILE_NOTICE = false

/** 들어오자마자 뜨는 안내. 방문자가 닫는다. */
export const MOBILE_NOTICE = {
  title: '휴대폰에서는 목록 보기로 열립니다',
  body: `이 사이트는 3D 월드를 접목한 포트폴리오입니다.
  모바일에서는 주요 화면만 보여드리고 있습니다.
  PC에서 접속 시 3D 월드를 체험할 수 있습니다.`,
  action: '목록 보기 열기',
}

/** 안내를 닫았는데 세로로 들고 있을 때 이어서 뜨는 안내. 가로로 돌리면 스스로 걷힌다. */
export const ROTATE_NOTICE = {
  title: '화면을 가로로 돌려 주세요',
  body: '이 사이트는 가로 화면에 맞춰 만들었습니다.',
}
