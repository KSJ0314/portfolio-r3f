/** 버튼 한 변의 크기(px). 동그라미라 지름이기도 하다. */
export const CORNER_BUTTON_SIZE = 48

/** 화면 모서리에서 들이는 거리(px). */
export const CORNER_BUTTON_MARGIN = 16

/** 나란히 놓인 버튼 사이의 간격(px). */
export const CORNER_BUTTON_GAP = 12

/** 좁은 화면(마우스 없는 기기)에서의 크기·여백·간격. 화면이 작아 그만큼 줄여 둔다. */
export const CORNER_BUTTON_SIZE_COARSE = 36
export const CORNER_BUTTON_MARGIN_COARSE = 10
export const CORNER_BUTTON_GAP_COARSE = 8
