/** 버튼 한 변의 크기(px). 동그라미라 지름이기도 하다. */
export const CORNER_BUTTON_SIZE = 48

/** 화면 모서리에서 들이는 거리(px). */
export const CORNER_BUTTON_MARGIN = 16

/** 나란히 놓인 버튼 사이의 간격(px). */
export const CORNER_BUTTON_GAP = 12
