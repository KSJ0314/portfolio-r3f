/**
 * 페이지 판을 액자 앞면에서 띄우는 거리(월드).
 *
 * 액자를 누르는 투명 판(`GALLERY_FRAME_HIT_LIFT`)보다 **앞**이라야 한다.
 * 뒤에 두면 그 판이 앞을 가려 페이지 안의 넘김 버튼이 눌리지 않는다.
 */
export const GALLERY_PAGE_LIFT = 0.03

/**
 * 페이지 바탕색.
 *
 * 조명을 받지 않고 톤 매핑도 타지 않는다 — 벽등이 닿는 정도에 따라 칸마다 밝기가 달라지면
 * 읽는 화면으로 쓸 수 없고, 적은 색이 그대로 나와야 나중에 2D로 옮겼을 때도 같은 화면이 된다.
 */
export const GALLERY_PAGE_BACKGROUND = '#f6f4ef'
