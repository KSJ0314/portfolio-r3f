/**
 * 문간 안쪽을 채우는 빛.
 *
 * 면 넷으로 이루어진다 — 열린 문을 덮는 문쪽 면 · 바닥 면 · 뒷면, 그리고 그 밑에 깔리는 카펫.
 * 빛을 내는 세 면은 같은 색이고, 문쪽 면과 바닥 면만 그러데이션으로 밝아진다.
 */
export const PROJECTS_DOOR_GLOW = {
  /** 빛 색. 문쪽 면·바닥 면·뒷면이 모두 이 색이다. */
  color: '#ffe98f',
  /** 카펫 색. */
  carpetColor: '#ba4345',
  /** 문짝 대비 입구 크기 배율. */
  scale: 1,
  /** 뒷면까지의 깊이(모델 좌표). */
  depth: 0.07,
  /** 밝아지는 정도(0~1). 1에 가까울수록 급격하게 밝아지고 옅은 구간이 짧아진다. */
  sharpness: 0.8,
  /**
   * 문쪽 면과 바닥 면이 안쪽으로 뻗는 길이(모델 좌표). **두 면이 같은 길이다.**
   * 면이 짧으면 그러데이션이 그 안에서 다 끝나 급격해진다. 넘친 부분은 뒷면에 가려 보이지 않는다.
   */
  length: 0.35,
  /** 카펫이 안으로 들어갈수록 어두워지는 정도(0~1). */
  carpetFalloff: 0.6,
}
