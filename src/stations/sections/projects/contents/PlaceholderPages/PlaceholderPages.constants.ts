/** 자리표시 페이지 장수. 넘김이 도는지 눈으로 보려는 것이라 몇 장이든 상관없다. */
export const PLACEHOLDER_PAGE_COUNT = 3

/** 글씨 크기와 자리(가로 1 기준). */
export const PLACEHOLDER_TEXT = {
  /** 프로젝트 이름. */
  titleSize: 0.07,
  /** 이름을 판 한가운데에서 올리는 거리. */
  titleY: 0.04,
  /** `1 / 3` 표시. */
  numberSize: 0.04,
  /** 번호를 판 한가운데에서 내리는 거리. */
  numberY: 0.06,
}

/** 글씨 색. 페이지 바탕이 밝아 진회색으로 둔다. */
export const PLACEHOLDER_INK = '#3a3a3a'

/** 번호는 곁들이는 값이라 물러나 있다. */
export const PLACEHOLDER_SUB_INK = '#8a8a8a'
