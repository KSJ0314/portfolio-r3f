/**
 * 누르는 판을 그림보다 넉넉히 잡는 배수.
 * 아이콘이 작아 그림 그대로를 누르게 두면 잘 눌리지 않는다.
 */
export const PAGE_LINK_HIT_SCALE = 1.6

/** 누르는 판을 그림보다 조금 앞에 둔다. 뒤에 두면 그림이 가려 클릭이 잡히지 않는다. */
export const PAGE_LINK_LIFT = 0.0015
