/** 바닥과 겹쳐 깜빡이지 않도록 띄운다. 페이지 내용과 같은 층이다. */
export const CAREER_EXIT_Y = 0.05

/** 배치 기본값. 눈으로 맞추는 값이라 HUD로 조절한다. */
export const CAREER_EXIT = {
  /** 아이콘 세로 크기. */
  size: 0.6,
  /** 오른쪽 끝에서 들이는 거리. */
  right: 0.2,
  /** 위 끝에서 내리는 거리. */
  top: 0.2,
}
