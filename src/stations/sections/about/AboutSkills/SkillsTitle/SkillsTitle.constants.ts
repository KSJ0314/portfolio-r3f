/** 바닥과 겹쳐 깜빡이지 않도록 살짝 띄운다. 공구함과 같은 층이다. */
export const SKILLS_TITLE_Y = 0.02

/** 제목 문구. */
export const SKILLS_TITLE_TEXT = '기술 스택'

/** 배치 기본값. 눈으로 맞추는 값이라 HUD로 조절한다. */
export const SKILLS_TITLE = {
  /** 글자 크기. */
  size: 0.6,
  /** 로고 중심에서 오른쪽으로 띄우는 거리. */
  gap: 1,
  /** 세로 보정. 양수면 위로 올라간다. */
  offsetY: 0,
}
