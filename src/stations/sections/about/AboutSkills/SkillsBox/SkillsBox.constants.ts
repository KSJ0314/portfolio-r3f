/** 바닥과 겹쳐 깜빡이지 않도록 살짝 띄운다. 안내 화살표·Intro 내용과 같은 층이다. */
export const SKILLS_BOX_Y = 0.02

/** 공구함 그림. 배경이 투명한 PNG라 그 모양대로 오려진다. */
export const SKILLS_BOX_URL = '/images/skills_box.png'

/** 배치·모양 기본값. 눈으로 맞춰야 하는 값이라 HUD로 조절한다. */
export const SKILLS_BOX_PLACEMENT = {
  /** 그림 세로의 월드 크기(여백 제외). 가로는 그림의 실제 비율에서 나온다. */
  height: 4,
  /** 영역 중심 기준 오프셋(월드 x, z). 영역을 옮기면 스티커도 따라온다. */
  x: 0,
  z: 2,
  /** 종이 테두리 폭 — 그림 짧은 변 대비 비율. */
  border: 0.025,
  /** 그림자 흐림 폭(같은 비율 기준). */
  shadowBlur: 0.025,
  /** 그림자가 오른쪽·아래로 밀리는 거리(같은 비율 기준). 0이면 사방으로 고르게 퍼진다. */
  shadowDistance: 0,
  /** 그림자 진하기. */
  shadowOpacity: 0.22,
}

/**
 * 활성 상태에서 로고처럼 놓일 자세. 눈으로 맞추는 값이라 HUD로 조절한다.
 *
 * 여백은 그림의 **중심** 기준이다. 그림의 실제 가로는 텍스처를 구워야 알 수 있어
 * 꼭지점을 코드로 맞추려면 로더 결과를 밖으로 빼야 한다.
 */
export const SKILLS_BOX_LOGO = {
  /** 평소 크기에 곱하는 배율. */
  scale: 0.3,
  /** 영역 좌상단에서 안쪽으로 들이는 거리(월드 x, z). */
  x: 1,
  z: 1,
  /** y축 회전(도). 양수가 반시계다. */
  rotation: 8,
}
