/** 페이지 넘김 문구. 첫 페이지에는 다음만, 마지막 페이지에는 이전만 둔다. */
export const PAGER_NEXT_TEXT = '다음'
export const PAGER_PREV_TEXT = '이전'

/** 배치 기본값. 눈으로 맞추는 값이라 HUD로 조절한다. */
export const SKILLS_PAGER = {
  /** 글자 크기. */
  size: 0.4,
  /** 오른쪽 끝에서 들이는 거리. */
  right: 0.7,
  /** 아래 끝에서 올리는 거리. */
  bottom: 0.4,
}
