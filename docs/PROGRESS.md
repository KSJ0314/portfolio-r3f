# 진행 상황 (PROGRESS)

> 이 문서는 프로젝트의 현재 진행 상황을 추적합니다. 마지막 업데이트: **2026-08-20**
> 전체 계획은 [PLAN.md](./PLAN.md) 참고.

## 현재 단계

**Phase 8 — 스테이션 상세 구현 진행 중** (About 3개 완료, `projects`는 정적 오브젝트와 활성 전환까지)

## 단계별 상태

> 아트(디자인·모델링·배치)는 의존성 순서로 확정: **아트 디렉션(4) → 맵/환경 베이스(5) → 스테이션 상세 구현(8)**. 기능은 임시 Box로 먼저 검증.

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 셋업 (Vite+R3F+TS, 테마, 기본 씬, 문서) | 🟢 완료 |
| 0.5 | 배포 파이프라인 (Vercel Git 연동) | 🟢 완료 |
| 1 | 맵 + 클릭 카메라 이동 | 🟢 완료 |
| 2 | 스테이션 + 상호작용 + 미니맵 | 🟢 완료 |
| 3 | 2D/3D 전환 (스테이션 활성화·상세) | 🟢 완료 |
| 4 | 아트 디렉션 확정 (팔레트·룩·에셋 소싱) ☆ | 🟢 완료 |
| 5 | 맵/환경 베이스 (바닥 — Phase 4에 흡수, 나머지 소멸·Phase 8 이관) ☆ | 🟢 완료 |
| 6 | Firebase 데이터 레이어 | 🟢 완료 |
| 7 | 콘텐츠 (Firestore) | 🟢 완료 |
| 8 | 스테이션 상세 구현 (정적 오브젝트 · 인터랙션 UI, 스테이션 5개 반복) ★ | 🟡 진행 중 |
| 9 | 인쇄/PDF 출력 (PrintView, 콘텐츠 전체를 한 번에) ★ | ⚪ 대기 |
| 10 | 폴리싱 (후처리·낮밤 연출·성능·접근성·SEO) | ⚪ 대기 |
| 11 | 캐릭터 + 페인팅 (후순위) | ⚪ 대기 |

범례: ⚪ 대기 · 🟡 진행 중 · 🟢 완료 · 🔴 막힘/이슈 · ☆ 아트 전용 단계

### Phase 8 스테이션별 체크리스트

> 정적 오브젝트 · 인터랙션 UI, 스테이션 5개 동일 구조. 인쇄는 Phase 9에서 한 번에 한다.

| 스테이션 | 정적 오브젝트 | 인터랙션 UI |
|---|---|---|
| about-intro (Intro) | 🟢 | 🟢 |
| about-skills (Skills) | 🟢 | 🟢 |
| about-career (Education, Awards & Certifications) | 🟢 | 🟢 |
| projects (Projects 구역 — 건물) | 🟢 | 🟡 (실내 로비까지. 전시대·체험 데모 남음) |
| guestbook | ⚪ | ⚪ (쓰기 폼·봇 방지·보안 규칙 포함) |

### Phase 0 세부 체크리스트

- [x] 레포 생성 (`git init`)
- [x] PLAN.md 작성
- [x] `docs/` 폴더 + 문서 골격 (PLAN/PROGRESS/LEARNING/DECISIONS/CONVENTIONS/ARCHITECTURE/FIRESTORE/DESIGN) + 루트 README
- [x] 커밋/브랜치(Git Flow)/배포 컨벤션 확정 (CONVENTIONS)
- [x] 첫 커밋(init) + `master`→`main` 변경
- [x] GitHub CLI 설치 + 인증
- [x] GitHub public 레포 생성 + push → https://github.com/KSJ0314/portfolio-r3f
- [x] 환경 확인 (`node -v`, `npm -v`)
- [x] Vite 스캐폴딩 (React + TS)
- [x] 의존성 설치 (three, R3F, drei, postprocessing, zustand, gsap, styled-components 등)
- [x] 기본 폴더 구조
- [x] ThemeProvider + GlobalStyle + light/dark 테마 토큰
- [x] 기본 3D 씬 (Canvas + 조명 + 바닥) + 테마 토글
- [x] `npm run dev` 구동 확인

## 작업 로그

- **2026-06-29**
  - 전체 기획 확정, PLAN.md 작성
  - 레포 생성 및 git 초기화
  - 문서 폴더(`docs/`) 구성 — PLAN.md 이동, 문서 골격 일괄 생성(PROGRESS/LEARNING/DECISIONS/CONVENTIONS/ARCHITECTURE/FIRESTORE/DESIGN), 루트 README
  - 커밋/브랜치(Git Flow)/배포 컨벤션 확정
  - 첫 커밋(init) + `master`→`main`, GitHub CLI 설치, public 레포 생성 + push (https://github.com/KSJ0314/portfolio-r3f)
- **2026-07-01**
  - **환경 & 스캐폴딩**: 환경 확인, Vite(React+TS) 스캐폴딩, 핵심 의존성 설치(three · @react-three/fiber(v9) · drei · postprocessing · zustand · gsap · styled-components), package 이름 `portfolio-r3f`
  - **폴더 구조**: `src/{scene, features, ui, state, lib, content, theme, styles}` 생성
  - **테마**:
    - `src/theme/themes.ts` — light/dark 테마 토큰 (2D 색상 + 3D 씬 색상·조명값 포함)
    - `src/theme/styled.d.ts` — styled-components `DefaultTheme` 타입 보강
    - `src/styles/GlobalStyle.ts` — 전역 리셋 + 테마 배경/글자색 + 전환 애니메이션
    - `src/state/useThemeStore.ts` — zustand 테마 스토어 (mode, toggle)
    - `src/ui/ThemeToggle/` — 테마 토글 버튼 컴포넌트 (index / tsx / styled)
  - **앱 배선**: `src/App.tsx`(ThemeProvider + GlobalStyle + Experience + ThemeToggle), `src/main.tsx`(불필요한 `index.css` import 제거)
  - **기본 3D 씬**: `src/scene/Experience/` — Canvas + ambient/directional 조명(테마 연동) + 바닥 plane + 임시 박스 + 임시 OrbitControls (Phase 1 클릭 이동으로 대체 예정)
  - **트러블슈팅·결정**: `@vitejs/plugin-react` v6가 oxc 사용(`babel` 옵션 없음) 확인 → `babel-plugin-styled-components` 제거, `vite.config.ts` `react()`로 원복
  - **정리**: 미사용 보일러플레이트 삭제(`index.css`·`App.css`·`src/assets/*`·`public/icons.svg`), `index.html`(title `3D Portfolio: SoJung Kim`, lang `ko`), 채워진 폴더 `.gitkeep` 제거
  - **브랜치 전략 확장**: 풀 Git Flow(main+develop+feature, 운영 시 release·hotfix)로 결정, `develop` 브랜치 부트스트랩(main·develop baseline 동기화 + 원격 push)
  - **배포처 결정**: GitHub Pages → Vercel (main=프로덕션 / develop=스테이징 / PR=프리뷰), Phase 0.5에서 연결
- **2026-07-03**
  - **Phase 0.5 — Vercel 배포 연결**: Vercel Git 연동으로 배포 파이프라인 구성 (별도 CI 워크플로 불필요, Vite 자동 감지: build `npm run build` / output `dist`)
  - **환경 매핑 확정**: `main`=프로덕션 / `develop`=스테이징 프리뷰 / PR=자동 프리뷰 (레포 default 브랜치를 프로덕션으로 자동 지정)
  - **프로덕션 라이브 URL**: https://portfolio-r3f-blue.vercel.app/
- **2026-07-07**
  - **Phase 1 — 맵 + 우클릭 카메라 이동** 구현
  - **이동 방식**: 우클릭 홀드로 캐릭터가 목적지로 이동. 누른 동안 매 프레임 커서 밑 바닥 지점을 레이캐스트해 목표점 갱신 → 커서가 정지해 있어도(카메라가 따라가며 월드가 밀리므로) 계속 이동, 떼면 정지. 좌클릭은 인터랙션용 예약. 거리와 무관한 **고정 속도**(매 프레임 step).
  - **카메라**: 캐릭터와의 상대 오프셋(아이소메트릭 각도·거리)을 고정한 채 매 프레임 캐릭터를 따라가 **화면 중앙 고정**(캐릭터 팔로우). 원근 제거 위해 **Orthographic 카메라**(zoom 기반) 채택 — 춘식이식 평면 아이소메트릭 뷰.
  - **월드/경계**: 임시 진한 초록 바닥(테스트 가시성) + 테스트용 격자(drei `Grid`, raycast 제외). 이동 범위를 `CAMERA_BOUNDS`로 clamp → 경계에서 투명 벽처럼 정지, 맵을 크게 둬 가장자리 미노출. 저폴리/파스텔 디자인·텍스처는 후속(바닥 전체 적용 예정).
  - **컴포넌트 구조**:
    - `src/state/useCameraStore.ts` — 공유 상태(position/target/setTarget, 경계 clamp)
    - `src/scene/CameraRig/` — 카메라 팔로우(오프셋 유지 + lookAt)
    - `src/scene/Character/` — 임시 캐릭터(박스) + 매 프레임 고정 속도 이동
    - `src/scene/World/` — 바닥·격자 + 우클릭 입력·레이캐스트
    - `Experience` — Orthographic Canvas + 우클릭 메뉴 차단, 임시 박스·OrbitControls 제거
  - **트러블슈팅**: 새로고침 시 가끔 맵 미표시 — 오프셋 초기화를 passive `useEffect`로 해 첫 `useFrame`이 먼저 돌면 offset이 (0,0,0)으로 굳어 카메라가 원점에 박히는 레이스. `useLayoutEffect`(첫 프레임보다 먼저 실행) + `ready` 가드로 해결.
- **2026-07-09**
  - **Phase 2 — 스테이션 + 상호작용 + 미니맵** 구현 (표지판 렌더는 아트 단계로 보류)
  - **스테이션 데이터**(`src/content/stations.ts`): 섹션(About/Projects/Guestbook) + 스테이션 7개(About 3 · 프로젝트 플레이스홀더 3 · Guestbook 1) + 표지판·HUB. 겉모습·상세는 데이터가 아니라 `id→전용 컴포넌트` 레지스트리 담당(임시 배치 좌표, 이후 조정).
  - **렌더 + 근접 판정**(`src/scene/Stations/`): `Stations`가 매 프레임 `useFrame`으로 캐릭터-스테이션 근접(반경 3) 판정 → `nearId`. `Station`은 정적 박스 + 라벨(drei `Html`) + 좌클릭 선택. 시각 연출(떠오름 등)은 넣지 않음 — 각 스테이션 고유 오브젝트로 이후 교체.
  - **상호작용 상태**(`src/state/useStationStore.ts`): `nearId`/`activeId`. **선택은 근접한 스테이션에만** 가능하고, 근접 범위를 벗어나면 자동 해제.
  - **미니맵**(`src/ui/Minimap/`): 원형·플레이어 중심·주변 16유닛 표시. 화면 각도에 맞춰 회전(하드코딩 아님 — 카메라에서 유도한 `viewAngle` 사용), 스테이션 short 라벨·근접/선택 표시. 이동은 rAF로 마커만 갱신(리렌더 없음).
  - **개발용**: `DebugHUD`(dev 전용, `import.meta.env.DEV`) — pos/target/speed/view/near/active. 카메라 스토어에 `viewAngle`(CameraRig가 유도)·`motion.speed`(Character가 기록) 추가.
  - **트러블슈팅**: 근접 판정이 안 되던 문제 = **Vite HMR로 R3F `useFrame` 콜백이 낡아 멈춘 것**(코드는 정상, 개발 서버 재시작/하드리프레시로 해결). react-dom rAF로 우회했다가 원인 확정 후 정통 R3F(`Stations` `useFrame`)로 원복.
  - **트러블슈팅**: import 빨간줄 = 폴더 케이싱 충돌(`src/scene/stations` vs `Stations`, TS1261). 폴더를 PascalCase `Stations`로 통일 + 불필요 `.gitkeep` 제거.
  - **설계 문서 정비**: "스테이션=고유 3D 오브젝트+제자리 활성화" 모델로 PLAN/ARCHITECTURE/DESIGN/DECISIONS(006) 갱신. 아트(디자인·모델링·배치) 의존성 순서를 로드맵에 명시 — 아트 디렉션(4)→맵/환경(5)→스테이션 디자인(8), 번호 재정렬(총 13단계).

- **2026-07-13**
  - **Phase 3 — 2D/3D 전환(스테이션 활성화 프레임워크)** 구현. 상세·연출은 스테이션마다 다르므로 이 단계에선 **판만 깔고 실제 상세는 만들지 않음**.
  - **활성화 라이프사이클**(`src/state/useStationStore.ts`): `idle → entering → active → exiting` 상태 머신. 이동 잠금은 **애니메이션 재생 중(entering·exiting)에만** 걸린다. `active`에서 우클릭은 곧 종료 트리거이고, 클릭 지점(`pendingTarget`)을 기억해뒀다가 **종료 애니메이션이 끝난 뒤** 캐릭터가 그리로 출발한다. ESC로도 종료. 좌클릭은 상세 내부 요소 상호작용 전용이라 닫기에 쓰지 않음.
  - **카메라 제어권 위임**(`CameraRig`): 활성화되면 팔로우를 멈춰 **카메라를 스테이션에 통째로 넘긴다**. 공통층은 어떻게 움직이는지 알지 못하고, 닫힐 때 원래 오프셋·zoom으로 **복귀만 보장**한다. 기본 트윈·선언형 스펙조차 두지 않음 — 있으면 이후 스테이션이 그 틀에 끌려가므로. (DECISIONS 007)
  - **레지스트리**(`src/features/stations/`): `id → { Scene?, Overlay? }`(Canvas 안 3D / Canvas 밖 DOM). 공통 셸·기본 구현체 없음. 마운트 슬롯은 `ActiveStationScene`(Canvas 안)과 `StationLifecycle`(Canvas 밖, ESC 종료 + 미구현 스테이션 즉시 완료 fallback).
  - **계약**: 스테이션 구현이 `phase`를 받아 애니메이션을 재생하고, 끝나면 `enterComplete()`/`exitComplete()`로 공통층에 알린다. `about-intro`에 **임시 뼈대**를 등록해 라이프사이클이 도는지 확인(그리는 것 없이 애니메이션 자리에 1초 지연만).
  - **DebugHUD**: `phase`·`locked`·`camera`(follow/station) 추가. 이 단계 검증은 전부 HUD 상태값으로.
  - **트러블슈팅**: 우클릭 홀드로 이동 중 스테이션 좌클릭이 안 먹힘 = `pointerdown`이 포인터 활성 전환 시에만 발생하는 것이 원인. 우클릭으로 이미 활성이라 좌클릭은 `buttons`만 바뀐 `pointermove`로 옴. R3F 이벤트가 전부 pointer 기반이라 무력 → `Stations`가 캔버스 `mousedown`을 직접 듣고 레이캐스트해 해결.
  - **트러블슈팅**: eslint `react-hooks/immutability`가 `useEffect`/`useFrame` 안에서 훅 반환값(`useThree().camera`)의 프로퍼티 대입(`cam.zoom = …`)을 막음 → `useFrame((state) => …)`이 넘겨주는 `state.camera`를 사용해 해결.
  - **주석 규칙 정리**: 한국어 주석 줄바꿈을 절 경계 기준으로 통일(`src/` 전체). JSDoc은 선언 문서, `//`는 몸통 안 이유 메모로 역할 구분.
  - **남긴 한계**: 평상시 겉모습(`Station`의 임시 박스)과 활성 시 상세 컴포넌트가 분리돼 있어 같은 오브젝트가 이어서 변형되는 연출은 아직 불가. 겉모습 슬롯은 고유 오브젝트를 만드는 Phase 8에서 레지스트리에 추가.

- **2026-07-14**
  - **Phase 4 — 아트 디렉션 확정**. 저폴리 파스텔 마을 → **스케치북**으로 전환 (DECISIONS 008).
  - **컨셉**: 월드는 빈 종이. 마을·나무·집·하늘 없음. 스테이션은 종이 위의 그림이고 활성화하면 3D로 전환되며, 전환 연출은 스테이션마다 따로 만든다. 테마는 조명 색·밝기 변화만(저녁 노을 정도), 네온은 후순위.
  - **바닥(모눈종이)**: 손그림 모눈 텍스처를 코드로 생성해 PNG로 굽고 타일링 (DECISIONS 009). 원래 Phase 5(맵/환경 베이스) 몫이나, 아트 방향을 눈으로 확인하려 앞당겨 완성. 생성 로직·기본값을 `src/lib/gridPaper/`에 두고 스크립트(`scripts/generate-grid-paper.mjs`)와 앱이 함께 쓴다. 심리스는 떨림·농담을 타일 폭의 정수 주기 사인파로만 만들어 보장. 선 굵기·떨림은 칸 크기 대비 비율이라 칸 수만 바꿔도 모양이 유지된다.
  - **개발용 HUD**: leva 도입(`GridPaperHUD`, dev 전용·우하단·접힘). 슬라이더로 텍스처 값을 실시간 조절하고, 조작 중엔 저해상도·멈추면 고해상도로 다시 굽는다. 확정 값은 "값 복사(JSON)" → 상수 반영 → 스크립트로 재생성.
  - **바닥 재질**: 조명을 받지 않는 basic + 톤 매핑 제외 (DECISIONS 010). 조명을 받게 두면 광량이 1에 못 미쳐 흰 종이가 회색으로 찍힌다.
  - **폰트**: 손글씨 감자꽃 + 본문 Pretendard, 둘 다 self-host. 테마에 `fonts` 토큰 추가. 씬 안 3D 텍스트는 웹폰트가 아니라 ttf를 읽으므로 상용 한글 2350자로 서브셋(약 1MB).
  - **맵 좌표 재배치**: 맵 기준 오른쪽=About · 아래=Projects · 왼쪽=Guestbook · 위=빈 공간. 카메라 방위각을 그에 맞춰 돌려 화면 구도(About이 우측 상단)를 유지. 줌 120→85, 고도각 약 42°.
  - **테마 색 정리**: `scene.background`를 하늘색에서 종이 밖 여백으로, 죽은 값이 된 `scene.ground` 제거, UI 배경·테두리를 종이 톤으로.
  - **주석 규칙**: 일기·경위 서술 금지, 짧게, 멀쩡한 어미는 통일한다고 바꾸지 않기 (CLAUDE.md).

- **2026-07-15**
  - **Phase 5 — 맵/환경 베이스 완료 처리.** 실체인 모눈종이 바닥은 Phase 4에서 아트 방향을 눈으로 검증하려 임시 구현하다 완성돼, Phase 5로 새로 할 일이 남지 않는다.
  - **길·경계벽·분위기 소품 소멸**: 스케치북 전환으로 월드는 빈 종이. 길 없이 오픈월드처럼 배치하고, 방향 안내(바닥 화살표·표지판)는 스테이션 실물이 생긴 뒤라야 의미가 있다.
  - **경계벽 불필요**: 기존 `CAMERA_BOUNDS` clamp가 곧 최종. 바닥을 이동범위보다 크게 둬 가장자리가 안 보이므로 별도 물리 경계벽을 두지 않는다 (DECISIONS 005 갱신).
  - **Phase 8 이관**: 스테이션 최종 배치·스케일·표지판·바닥 화살표는 스테이션별 상세 구현에서 각자 정한다.

- **2026-07-16**
  - **Phase 6 — Firebase 데이터 레이어** 구현·연결. 앱↔Firestore 연결과 읽기·쓰기 접근 함수를 이 단계에서 완성. 실제 데이터 입력·화면 연결은 Phase 7.
  - **초기화·데이터 접근**(`src/lib/firebase/`): `initializeApp` + `getFirestore`. 읽기는 커스텀 훅(`useCollection`/`useDoc`, `{ data, loading, error }`), 콘텐츠 5종 입력은 개발용 쓰기 함수(`setDocData`/`addDocData`). 방명록 쓰기·App Check는 Phase 11.
  - **컬렉션 8개**(profile·skills·experiences·education·awards·spec·projects·guestbook): Firestore는 빈 컬렉션을 못 만들어 `scripts/seed-firestore.mjs`가 각 컬렉션에 빈 `_placeholder` 문서를 넣어 생성. config는 `.env.local`에서 읽어 하드코딩 안 함. 필드 설계는 Phase 7.
  - **스테이션 매핑 + 활성화 시 읽기**: 각 스테이션에 읽을 컬렉션을 매핑(`stations.ts`의 `collections`), 활성화되면 `fetchCollection`으로 읽어 콘솔에 확인 출력. 데이터 활용은 스테이션 상세 구현에서(콘솔 출력은 그때까지 유지).
  - **보안 규칙**: 콘솔 관리. 개발 중엔 열린 규칙, 배포 전 잠금(콘텐츠 write 차단, guestbook은 Phase 11) — DECISIONS 011. web config는 공개값이라 env는 비밀이 아니라 환경 분리용.
  - **Phase 7 — 콘텐츠** 스키마 확정 + 데이터 입력. 컬렉션별로 순서대로 진행: 스키마 제안 → 확정 → 실제 값 입력. 일회성 스크립트로 컬렉션마다 데이터를 추가하고 `_placeholder` 문서를 삭제(스크립트는 작업 후 정리, 결과는 콘솔에서 확인).
    - **profile**: 1건. **skills**: 7건(React·Kotlin·SpringBoot·Electron·ThreeJS·ReactNative·Vue3, `order`는 전체 순위 하나로 카테고리 필터 후 정렬도 겸함). **experiences**: 1건(`order` 없이 `startDate` 기준 정렬). **education**: 2건(학교가 아니라 SSAFY 등 교육 과정이라 `school/major` 대신 `institution/program`으로 재구성). **awards**: 3건. **spec**: 4건.
    - **projects**: 3건. Firebase가 다른 프로젝트와 공유하는 DB라 `{ title, summary, startDate, endDate, link, order }`만 두고, `tech`·`images`·`demoKey` 등 상세는 로컬로 분리(PLAN.md 초안 예시와 다름 — DECISIONS 012).
  - **로드맵 문구 정정**: PLAN.md Phase 7 항목에 있던 "스테이션 상세"가 Phase 8 몫("활성화 상세를 레지스트리로 교체")과 겹쳐 보여 혼동을 일으킴. Phase 7은 데이터 입력까지로, "스테이션 상세 컴포넌트 구현"은 Phase 8로 문구를 정리(FIRESTORE.md 스키마 문서화 포함).
  - **남긴 한계**: `skills`의 ThreeJS 항목은 description이 요약 1줄만 있음. 상세 내용은 이 포트폴리오 프로젝트 완성 후 보강 예정.

- **2026-07-22**
  - **Phase 8 — `about-intro` 스테이션 구현**(정적 오브젝트 + 인터랙션 UI). 로드맵에서 인쇄를 떼어내 Phase 9로 분리하고, Skills를 Intro에서 빼 별도 스테이션으로 돌린다 (DECISIONS 013 갱신).
  - **레지스트리 계약 확장**(`features/stations/registry.ts`): 비활성 상태를 그리는 `Inactive` 슬롯과 근접 거리 계산을 맡기는 `distanceTo`를 추가. `Station`은 등록된 스테이션이면 공통 임시 박스 대신 그 구현을 그린다. Phase 3이 한계로 남겨둔 "비활성 슬롯 없음"이 이걸로 풀린다.
  - **스테이션 영역**(DECISIONS 014): 종이 위 사각형이 곧 클릭 판정 범위이자 근접 판정 기준. 클릭은 영역 전체를 덮는 투명 판이 받고(포인터 핸들러가 없어 우클릭 이동은 그대로 통과), 근접은 중심점이 아니라 **테두리까지의 최단거리**로 잰다. Intro 영역은 11 × 6.5.
  - **Intro 페이지 내용**: Firestore `profile/main`에서 `tagline`·`intro`만 쓴다. tagline은 손글씨로 상단 가운데, intro는 Pretendard로 좌측 정렬 + 마크다운 인용구 같은 세로 막대, 사진은 하단 가운데. 내용을 담은 그룹을 눕혀 안쪽을 화면 좌표로 다룬다.
    - 인용 막대 높이는 본문이 몇 줄로 접힐지 미리 알 수 없어, troika가 배치를 끝내고 주는 경계값으로 잰다.
    - 사진 비율은 상수로 두지 않고 불러온 텍스처의 실제 픽셀 크기에서 계산한다 — 사진을 갈아끼워도 코드를 고칠 일이 없다.
    - 글씨·사진·막대는 레이캐스트에서 뺀다. 그래야 밑의 클릭 판정면이 잡힌다.
  - **Pretendard ttf 서브셋**(`scripts/subset-pretendard.py`): 3D 텍스트는 웹폰트가 아니라 ttf를 읽으므로 Regular·Bold를 상용 한글 2350자로 구웠다(각 2MB). 2350자는 euc-kr로 인코딩되는 음절을 뽑아 만든다. 3D 텍스트용 폰트 경로는 `content/fonts.ts`로 모은다.
  - **활성 연출 = 카메라 각도 전환**(DECISIONS 015): Intro는 내용이 바닥에 그려져 있으므로 오브젝트를 건드리지 않고 카메라만 돌린다. 정면(수직)에서 내려다보면 웹페이지처럼 읽히고, 닫으면 항공뷰로 돌아간다. 배율은 건드리지 않는다.
    - 수직 시선에서는 기본 up(+y)이 시선과 겹쳐 화면 방향이 정해지지 않아, up을 월드 -z로 고정해 영역 축과 화면 축을 맞춘다.
    - 돌아갈 항공뷰 자세는 현재 카메라에서 캡처하지 않고 **팔로우 규칙(캐릭터 + 오프셋)으로 매번 계산**한다. 이를 위해 `useCameraStore`에 `followOffset`을 두고 `CameraRig`가 초기화할 때 기록한다.
    - **사이트 첫 화면이 Intro 활성 상태**다. 진입 애니메이션 없이 처음부터 정면뷰로 시작한다.
  - **닫는 방법 교체**(DECISIONS 007 갱신): 우클릭 종료를 폐기하고 **근접 이탈 · 나가기 요소 클릭 · ESC** 세 가지로. 활성 중에도 평소처럼 이동할 수 있고, 걸어서 멀어지는 것이 곧 닫기다. 이동 잠금은 진입 애니메이션 중에만 건다 — 종료 중에 잠그면 걸어나가던 캐릭터가 멈칫한다. 쓸모없어진 `pendingTarget` 제거.
  - **크레파스 획 유틸**(`lib/crayon/`, DECISIONS 016): 크레파스는 선이 아니라 종이 결에 걸린 왁스 알갱이라, 경로를 촘촘히 훑으며 굵기 안쪽에 알갱이를 흩뿌린다. 가장자리 확률을 낮춰 테두리를 너덜하게, 진하기를 저주파로 흔들어 필압을, 별도 마스크로 끊김을 만든다. 씨앗 기반 난수라 렌더할 때마다 모양이 같다. 끝은 반지름만큼 더 나가되 폭을 원의 단면으로 줄여 둥글게 맺는다. 나가기 화살표를 이 유틸로 구워 쓴다.
  - **개발용 HUD**: `IntroPageHUD`(leva, dev 전용) + `useIntroPageStore`로 영역·여백·글씨·사진·화살표를 실시간 조절하고, 값 복사로 상수에 반영하는 흐름. leva 패널을 좌상단으로 옮기고 제목을 "개발 튜닝"으로, 모든 폴더를 접힌 채 시작하게 했다.
  - **정리**: 테마 토글 제거(밤 테마가 종이만 어두워지는 상태라 완성 시 복원), `World`의 폰트 확인용 HOME 텍스트 제거, `Stations`의 Firestore 콘솔 출력(Phase 6 확인용) 제거.
  - **배치 값**: `about-intro`를 원점으로 옮기고 나머지 스테이션은 겹치지 않게 임시 이동. 캐릭터 시작 위치를 `[0, 5]`로 상수화(`CHARACTER_START`)하고 카메라 초기 위치도 같은 만큼 옮겨 시점을 유지. 이동 속도 8→4, 근접 반경 3→2.
  - **트러블슈팅**: 비활성화할 때 카메라가 각도만 돌고 위치는 튀는 문제 = 항공뷰 자세를 "지금 카메라"에서 캡처한 탓. R3F는 카메라 위치만 넣고 방향은 돌리지 않으며, 첫 화면은 Intro가 활성이라 팔로우가 한 번도 돌지 않는다. 자세를 계산으로 바꿔 해결.
  - **남긴 한계**: 첫 진입 시 캐릭터를 숨겼다 등장시키는 연출은 실제 캐릭터를 만드는 Phase 11로 미룬다. 밤 테마는 종이만 어두워지고 그 위 요소는 밝게 남는다 — 조명 체계로 되돌릴지 후처리로 덮을지는 스테이션을 다 만든 뒤 정한다. `about-skills` 스테이션은 아직 신설하지 않았다.

- **2026-07-23**
  - **컨벤션 정비 리팩터**(코드 이동·분리, 동작 변화 없음). 컴포넌트 전용 폴더 + index 배럴 · 타입 파일 분리 규칙에 맞춰 전체를 정돈했다.
    - **stations 승격**: `src/features/` 제거 → `src/stations/`. 섹션 구현을 `sections/{about,projects,guestbook}/`로 그룹화하고, 프레임워크(registry·ActiveStationScene·StationLifecycle·useActiveStation·공용 `types`)는 `stations/` 바로 밑에 둔다.
    - **AboutIntro 분해**: 한 파일에 몰려 있던 구현을 `sections/about/AboutIntro/`로 쪼갰다(AboutIntroInactive·AboutIntroScene·ProfilePhoto·ExitArrow·`.types`·`.constants`·`.distance`).
    - **DevHUD 통합**: 흩어져 있던 개발용 HUD(DebugHUD·GridPaperHUD·IntroPageHUD)를 `ui/DevHUD/` 하위로 모으고 `App`은 `<DevHUD />` 한 줄로 렌더한다. dev 게이트는 App에 남겨 프로덕션 트리셰이킹을 유지한다.

- **2026-07-24**
  - **크레파스 스튜디오**(개발용 저작 도구, `src/tools/CrayonStudio/`) 구현. 손그림 요소의 획 좌표를 코드에 손으로 박던 것을 마우스로 그려 뽑도록 바꾼다 (DECISIONS 017). 스테이션 진행이 아니라 도구라 체크리스트·다음 할 일은 건드리지 않는다.
  - **증분 렌더 전환**(`lib/Crayon/Crayon.draw`): 마우스로 긋는 동안 실시간으로 보이도록 늘어난 구간만 덧그린다. 무늬 진행도를 획 전체 대비 비율에서 **따라간 거리** 기준으로 바꿔, 획이 길어져도 앞부분 무늬가 변하지 않게 했다. 난수 스트림을 획 단위로 이어 소비해 나눠 그린 결과가 한 번에 그린 것과 동일함을 확인(알갱이 좌표·알파까지 일치). 나가기 화살표 무늬가 미세하게 달라졌으나 눈에 띄지 않아 그대로 둔다.
  - **공유 두 겹 분리**(`lib/Crayon/freehand/`): 획을 쌓아 두는 캔버스(`Crayon.canvas`)와 포인터 궤적 수집(`Crayon.input`). 좌표 변환을 주입받아, 훗날 방문자가 바닥에 낙서하는 재미 요소로 확장할 때 좌표 변환만 갈아끼우면 되게 했다.
  - **획별 색**: `CrayonStroke`에 선택적 `color` 추가. 한 그림에 여러 색을 섞을 수 있고, 없으면 공유 색을 쓴다(단색 데이터 그대로 유효). `<Crayon>`도 여러 색 그림을 그대로 받는다.
  - **도구 UI**: 색·굵기·손떨림·진하기·거칠기·끊김 조절(굵기·손떨림은 비율이라 크기 무관), 획 단위 지우개, 실행 취소·전체 지우기·질감 다시 그리기, PNG 저장(모눈종이는 판의 CSS 배경이라 획만 투명 배경으로 남음), `CrayonDrawing`·`<Crayon>` props 복사(개발 모드 전용). 크레파스·지우개 커서(크레파스는 고른 색을 따라감), 속성 값은 localStorage에 보관·복원(필드별 검증).
  - **라우터 도입**: `react-router-dom`으로 `/`(3D 포트폴리오)와 `/crayon`(스튜디오 단독) 분리. 테마·전역 스타일을 `Root`로 올려 공유. 단독 페이지는 모달 껍데기를 벗고 화면을 채우며 배포본에도 포함된다. `vercel.json`으로 SPA rewrite(직접 접속 404 방지).

- **2026-07-25**
  - **크레파스 스튜디오 내보내기 프레임** 도입. 점선 사각 프레임이 그림과 분리된 채 내보낼 영역을 정한다. 변·모서리 드래그로 가로세로를 따로 조절하고, 프레임을 옮기거나 키워도 그린 획은 변형되지 않는다. JSON 좌표는 프레임 기준 0~1로 재정규화하고 PNG는 프레임으로 크롭해 여백을 조절하며, strokeWidth도 프레임 가로 대비로 환산한다. 기본 여백은 판 짧은 변 기준이라 판이 직사각형이어도 사방 픽셀 여백이 균일하다.
  - **크레파스 클릭으로 점 찍기.** 점 1개(클릭)도 원형 점으로 남도록, 획 끝 둥근 마무리를 좌·우로 붙여 원을 만든다. CrayonCanvas가 클릭을 버리지 않고 점으로 저장하고, 지우개·되돌리기·`<Crayon>` 렌더에서도 일관되게 나온다.
  - **intro 나가기 화살표 교체.** 임시로 둔 직선 3획 화살표를 크레파스 스튜디오로 직접 그린 곡선 2획 화살표로 바꾼다.

- **2026-07-26**
  - **Phase 8 — `about-skills` 스테이션 신설**(정적 오브젝트 진행 중). 영역 10 × 8을 좌상단 (6, 6) 기준으로 두고
    클릭 판정면·영역 테두리까지의 근접 거리를 레지스트리에 등록. 배치 좌표(`stations.ts`)는 좌상단 + 반크기로
    계산해 크기를 바꿔도 꼭지점이 유지된다. 종이 위에 놓일 실제 모습은 아직 없다.
  - **Skills 안내 화살표**(`SkillsGuideArrow`): 캐릭터 시작 자리에 놓여 Skills 쪽을 가리키는 바닥 화살표.
    좌표는 그림의 좌상단 꼭지점이고 회전축도 그 점이다. **Intro가 닫혀 카메라 전환이 끝난 뒤**(라이프사이클이
    처음 idle이 되는 시점) 나타나며, 그때 크레파스로 긋듯 그어진다. 한 번 나오면 계속 종이에 남는다.
  - **`<Crayon reveal>` — 그려지는 렌더 옵트인** (DECISIONS 018): 캔버스를 직접 들고 매 프레임 이어 그린 뒤
    `needsUpdate`로 갱신하고, 다 그리면 갱신을 멈춰 정적 텍스처가 된다. 등속은 획 길이를 미리 재
    "전체 길이 ÷ 시간"만큼 전진시키며, 구간 중간에서 멈출 땐 그 지점을 보간해 넘긴다.
    prop을 주지 않은 기존 사용처(나가기 화살표)는 그대로 한 번에 굽는다.
  - **크기 배율**: 기준 크기와 획 굵기에 같은 배율을 곱한다. 텍스처 픽셀 환산값이 상수라 그림을 대각선으로
    확대한 것과 같고, 크기를 바꿔도 텍스처를 다시 굽지 않아 그리던 연출이 끊기지 않는다.
  - **맵 장식 자리 분리**(`scene/MapDecorations/`): 스테이션에 속하지 않는 요소(길안내 화살표·표지판)의
    마운트 자리. Experience는 한 줄만 두고, Suspense도 요소마다가 아니라 이 공통 자리에 하나만 둔다.
  - **화살표별 폴더·상수 분리**: 획 좌표·크기·색처럼 그 화살표만의 값은 컴포넌트와 함께 둔다.
    `SkillsGuideArrow/`·`ExitArrow/` 하위 폴더에 `.constants.ts`(props가 있는 ExitArrow는 `.types.ts`도)를 둔다.
    컴포넌트 파일에 상수를 두면 스토어가 기본값을 읽을 때 순환 import가 된다.
  - **개발용 HUD**: Skills 패널에 안내 화살표 폴더(크기·좌표·회전·그리는 시간·다시 그리기) 추가.

- **2026-07-27**
  - **종이 스티커 유틸**(`lib/PaperSticker`, DECISIONS 019): 배경이 투명한 그림을 오려 붙인 종이로 굽는다.
    연필 그림은 가장자리가 흐리므로 알파를 기준값으로 잘라 실루엣을 만들고, 그 실루엣을 원 둘레를 따라
    겹쳐 그려 테두리 폭만큼 사방으로 넓힌다. 바깥에서 번져 들어가 닿지 못한 곳은 안쪽 구멍이라 메워
    종이 한 장으로 남긴다. 그림자는 종이를 그릴 때 함께 깔려 밑으로 들어간다. 길이 값은 전부 그림
    짧은 변 대비 비율이라 크기를 바꿔도 인상이 유지된다. 전용 로더로 감싸 `useLoader`(Suspense)에 태운다.
  - **공구함 스티커**(`SkillsBox`): Skills 영역에 놓이는 비활성 모습. 좌표는 영역 중심 기준이라 영역을
    옮기거나 크기를 바꿔도 따라온다. 클릭·이동 판정은 밑의 영역 판이 맡으므로 레이캐스트에서 뺀다.
    확정값 — 영역 좌상단 (6, 3), 스티커 (0, 2)·세로 4, 테두리 0.025, 그림자 흐림 0.025·거리 0·진하기 0.22.
  - **활성 연출**(`AboutSkillsScene`, DECISIONS 020): 카메라를 정면(수직)으로 돌리고 공구함은 0.3배로
    줄어 영역 좌상단으로 물러나 로고가 된다. 초점은 배치 좌표가 아니라 영역 중심이라 영역을 옮기면
    따라온다. 배율은 건드리지 않는다.
  - **두 연출은 겹치지 않고 차례로** 돈다 — 들어갈 때 카메라(1.5초) 다음 공구함(1초), 나올 때 공구함
    다음 카메라. 시간값을 공유 상수로 두고 자기 차례가 아닌 쪽은 지연·빈 구간으로 기다리며,
    라이프사이클 완료 신호는 전체 시퀀스를 아는 카메라 쪽이 낸다.
  - **로고 전환의 주인**: 활성 구현이 아니라 상시 마운트된 비활성 구현이 갖는다. 활성 구현에서 따로
    그리면 스티커가 두 개가 되고 전환이 끊긴다. 스토어를 구독해 gsap가 그룹만 건드리므로 리렌더는 없다.
  - **개발용 HUD**: Skills 패널에 공구함(크기·좌표·테두리·그림자)과 공구함 로고(크기 배율·여백·회전) 폴더 추가.

- **2026-07-28**
  - **Phase 8 — `about-skills` 인터랙션 UI 완료.** 활성 화면을 제목·목록·나가기로 채운다.
  - **제목**(`SkillsTitle`): 로고 옆에 손글씨로 "기술 스택". 활성일 때만 두고, 좌표는 로고 자리
    기준이라 영역이나 로고를 옮기면 함께 따라온다.
  - **기술 목록**(`SkillsPages`, DECISIONS 021): Firestore `skills`를 분류로 나눠 두 페이지로 넘긴다.
    제목·로고는 고정이고 이 영역만 갈린다. 열 구성은 페이지마다 정하며, 열을 하나만 적으면
    그 분류를 개수로 반 나눠 두 열에 늘어놓는다(1페이지 Frontend). 2페이지는 분류마다 한 열이다
    (왼쪽 Mobile · 오른쪽 Backend). 이름은 손글씨, 설명은 본문체로 줄마다 `-`를 붙인다.
  - **항목 쌓기**: 설명이 몇 줄로 접힐지 미리 알 수 없어, 각 항목이 배치를 끝내고 알려주는 높이로
    다음 항목 자리를 잡는다. 높이가 다 모이기 전에는 겹쳐 보이므로 그동안 감춘다.
  - **표시 여부를 데이터로**: `skills`에 `active` 필드를 추가하고 아직 내용이 얕은 ThreeJS를 껐다.
    필드가 없으면 표시로 다룬다 — 문서를 추가하며 빠뜨려도 조용히 사라지지 않게.
  - **레벨 별**(`SkillLevel`): 직접 그린 별을 종이 스티커로 굽고, 이름 폭을 재서 그 오른쪽에
    숙련도만큼 붙인다. 전부 같은 그림이라 텍스처 하나를 나눠 쓴다.
  - **페이지 넘김**(`SkillsPager`): 우측 하단에 한 번에 하나만 둔다 — 첫 페이지는 다음, 마지막은 이전.
    번호는 쓰지 않는다.
  - **나가기**(`stations/ExitSticker`, DECISIONS 022): 어느 스테이션에서나 쓰는 공용 종이 스티커.
    스테이션은 놓을 자리만 정한다.
  - **커서**(`stations/usePointerCursor`): 씬 안 메시는 CSS를 걸 대상이 없어 문서 커서를 직접 바꾼다.
    얹힌 채로 언마운트되면 되돌릴 기회가 없으므로 정리도 함께 건다.
  - **정리**: `PaperSticker`가 여백을 뺀 그림 크기도 돌려준다(스티커를 늘어놓을 때 간격 기준).
    troika 측정 타입을 `stations/types.ts`로 올려 스테이션끼리 물지 않게 했다.
    `PaperSticker` 배럴에서 내부 전용 export를 뺐다.
  - **개발용 HUD**: Skills 패널에 제목·목록·레벨 별·페이지 넘김·나가기 폴더 추가.
  - **첫 화면 로딩 정리**(LEARNING 2026-07-28): 항공뷰가 잠깐 보이다 카메라가 튀고, Intro가 늦게
    한꺼번에 뜨고, 바닥보다 Intro가 먼저 뜨던 문제를 함께 잡았다. 경계를 공통 마운트 자리마다
    하나씩 두되 준비 시점이 다른 것은 묶지 않고(사진은 자기 경계), 함께 보여야 하는 것만 골라
    기다리는 가림막(`ui/SceneGate` + `useSceneReadyStore`)을 뒀다. 바닥·캐릭터·Intro가 다 준비되면
    페이드로 걷고 다시 덮지 않으며, 시간을 넘기면 새로고침으로 재시도하다 횟수를 넘기면 그냥 걷는다.
  - **스테이션도 한 번에**: 여는 순간 X·다음 버튼만 먼저 떴다 사라지던 문제를 공통층에서 잡았다.
    서스펜드하지 않고 늦게 오는 것은 스테이션이 `useStationGate`로 알리고, 열쇠가 걸린 동안
    공통층이 상세를 **마운트한 채 감춘다**. 앞으로의 스테이션도 훅 한 줄만 걸면 되고, 기다릴 것이
    없으면 그대로 즉시 뜬다. 뒤늦게 굽는 일 자체를 없애려고 에셋은 앱이 뜰 때 미리 굽는다
    (`scene/AssetPreload` + 목록 `content/assets.ts` — 에셋을 추가하면 여기 한 줄).

- **2026-07-31**
  - **맵 장식 자리로 정리**: Skills 안내 화살표를 `stations/sections/about/AboutSkills/`에서
    `scene/MapDecorations/`로 옮긴다. 특정 스테이션으로 유도할 뿐 그 스테이션에 속한 요소가 아니다.
    기본 좌표가 `CHARACTER_START`를 참조하던 것도 실제 값으로 고정한다 — 캐릭터 시작 위치를 옮길 때
    그림이 따라가면 안 된다.
  - **우클릭 안내 아이콘**(`RightClickHint`): 우클릭으로 이동한다는 것을 처음 온 사람은 알기 어렵다.
    오른쪽 버튼을 칠하고 클릭 이펙트를 붙인 마우스 그림을 종이 스티커로 구워 안내 화살표 곁에 놓는다.
    **화살표를 다 그은 뒤에** 나타나고, 한 번이라도 우클릭으로 이동하면 사라진다.
    세션 한정이라 새로고침하면 다시 나온다.
  - **등장 조건 공용화**(`MapDecorations.hooks`): 두 장식이 공유하는 "Intro가 닫혀 카메라 전환이
    끝난 시점"을 `useAfterIntro`로 뽑는다. 지연 인자를 주면 그만큼 더 기다리고, 주지 않으면 타이머를
    거치지 않아 한 프레임도 밀리지 않는다.
  - **이동 신호**(`useCameraStore.hasMoved`): 이동 잠금을 통과한 우클릭만 세어 잠긴 상태의 헛클릭을
    빼고, 이미 켜졌으면 `set`을 건너뛰어 리렌더를 만들지 않는다.
  - **장식 전용 상태·HUD 신설**: `useMapDecorationsStore` + `MapDecorationsHUD`(leva "맵 장식" 그룹).
    Skills 페이지 상태·HUD에 얹혀 있던 안내 화살표 항목을 전부 이쪽으로 옮긴다.
  - **첫 SVG 에셋**: 종이 스티커는 캔버스에 그려 알파를 읽으므로 형식을 가리지 않는다. 다만 SVG는
    `width`/`height`가 곧 굽는 해상도라 반드시 명시하고, 투명 여백까지 판 크기에 들어가므로
    그림에 맞춰 자른다.
  - **임시 박스 제거**: 레지스트리에 구현이 없는 스테이션을 공통 임시 박스 + 이름 라벨로 그리던
    fallback을 걷는다. 배치 데이터는 그대로라 미니맵에는 남고, 구현을 등록하면 종이 위에 나타난다.

- **2026-08-03**
  - **Skills 진입에 캐릭터 이동 추가**: 활성화하면 캐릭터가 페이지 앞자리로 걸어가고, 도착한 뒤에
    카메라가 정면으로 돌고 그다음 공구함이 로고로 물러난다. 세 연출이 겹치지 않고 차례로 돈다.
  - **연출 이동**(`useCameraStore`의 `walking`·`walkTo`·`endWalk`): 진입 잠금 중에도 지정한 자리로는
    평소 속도로 걸어간다. 우클릭 입력은 그대로 막힌다. 도착 판정은 이동을 맡은 `Character`가 하고,
    닿으면 신호를 꺼 다음 차례로 넘긴다.
  - **카메라 제어권 이양 시점**: `CameraRig`가 활성화되는 순간이 아니라 연출 이동이 끝날 때 카메라를
    넘긴다. 걷는 동안은 평소처럼 캐릭터를 따라가고, 스테이션은 그 팔로우 자세를 그대로 이어받아
    정면으로 돈다.
  - **차례를 신호로**(`useSkillsSequenceStore`): 걷는 시간이 거리에 따라 달라져 지연(delay) 상수로는
    차례를 맞출 수 없다. 카메라가 다 돌면 활성 구현이 `logoTurn`을 켜고 공구함이 그 신호를 보고 움직인다.
  - **서는 자리**(`SKILLS_STAND`): 영역 중심 기준 상대 좌표라 영역을 옮기면 따라온다. Skills HUD에
    "캐릭터 자리" 폴더 추가. 나갈 때 자리를 되돌리지는 않는다.
  - **미니맵·크레파스 버튼은 비활성일 때만**: 스테이션이 열려 있는 동안에는 두지 않고, 완전히
    닫힌 뒤(`idle`)에 다시 나타난다. 미니맵 마커는 점을 줄이고 근접·선택 강조를 걷는다 —
    열려 있는 동안 안 보이므로 강조가 쓰일 자리가 없다.
  - **월드맵**(`ui/WorldMap`): 미니맵을 누르면 열리는 종이 한 장. 모눈은 안쪽에만 깔고 테두리를
    종이색으로 남겨 오려 붙인 스티커처럼 보이게 한다. 미니맵과 같은 각도로 돌고 이름은 똑바로 선다.
    원점(Intro)이 위에서 1/4 지점에 오도록 뷰박스를 내리고 20% 확대. 닫기는 배경 클릭·닫기 버튼·ESC.
  - **자리로 순간이동**(`useCameraStore.teleportTo`): 지도에서 스테이션을 고르면 그 자리로 캐릭터를
    옮기고 닫는다. 좌표 이동만 하고 활성화는 하지 않는다.
  - **스테이션 자리를 레지스트리 계약으로**(DECISIONS 023): `stand`(월드 좌표)를 등록하면 데려가는
    일은 공통 함수(`walkToStand`·`teleportToStand`)가 맡고, 언제 부를지는 스테이션이 정한다.
    값은 각 스테이션 상수에 둔다 — Skills는 `SKILLS_CENTER + SKILLS_STAND_OFFSET`, Intro는 `INTRO_STAND`.
    스토어로 실시간 튜닝하던 "캐릭터 자리" HUD 폴더는 걷는다.
  - **배치 좌표는 선택 값**: 임시로 박아 둔 좌표(career·award·project 1~3·guestbook)를 지운다.
    자리가 없는 스테이션은 씬·미니맵·월드맵·근접 판정에서 빠지고, 자리는 상세 구현에서 영역과 함께 정한다.
  - **크레파스 스튜디오 제목**: 모달에서 제목을 누르면 단독 페이지(`/crayon`)로 간다.

- **2026-08-04**
  - **career 진입 안내 장식**: Skills를 닫고 나오면 횡단보도가 그어지고 그 곁에 신호등이 선다.
    아직 놓이지 않은 `about-career` 쪽으로 건너가라는 안내다.
  - **크레파스 바깥 윤곽 마무리**(`lib/Crayon/Crayon.edge.ts`, DECISIONS 024): 획 하나의 좌우 가장자리는
    `roughness`가 이미 너덜하게 만들지만, 획을 겹쳐 면을 채우면 겹친 안쪽이 꽉 차 **바깥 윤곽만 딱 떨어진다.**
    그림 전체가 채울 영역을 그리기 전에 미리 재 두고, 알갱이를 찍을 때 그 자리가 얼마나 안쪽인지 보고
    바깥일수록 건너뛴다. 값밭을 미리 만드는 덕에 그려 나가는 연출에서도 첫 획부터 테두리가 적용된다.
  - **모서리를 각지게 남기기**: 안쪽인 정도를 사방으로 퍼지는 흐림 한 번으로 재면 모서리에서 두 변이
    함께 깎여 직각이 둥글어진다. 가로·세로를 따로 재 **낮은 쪽**을 쓰면 등고선이 사각이라 각지게 남는다.
    두 값을 곱하면 사방으로 흐린 것과 같아져 둥글어지므로, 그 사이를 오가는 값(`roundness`)을 둔다.
  - **횡단보도**(`scene/MapDecorations/Crosswalk/`): 크레파스 스튜디오에서 그린 그림을 상단 중앙 기준으로
    놓는다. 기존 장식이 좌상단 꼭지점을 기준으로 두는 것과 달라, 판 중심을 세로 반크기만큼 밀어 맞춘다.
  - **되돌아온 뒤에 그린다**: 닫는 방법이 근접 이탈이기도 해서 닫히는 시점에 캐릭터는 멀리 가 있을 수 있다.
    그 자리에서 바로 그으면 그리는 것이 화면 밖이라 보이지도 않는데 이동만 막힌다.
    **닫히기 시작하면 카메라 복귀와 함께** 캐릭터가 Skills 자리로 되돌아오고, 도착해야 그어진다.
    걷는 시간이 거리마다 달라 지연이 아니라 도착 신호를 보고 넘어간다.
  - **카메라를 쥔 동안에는 연출 이동으로 보내지 않는다**: 종료 애니메이션 중에는 카메라 제어권이 아직
    스테이션에 있다. 이때 `walkTo`로 보내면 "연출 이동 중에는 평소처럼 따라간다"는 규칙에 걸려
    팔로우가 되살아나고 복귀 트윈과 부딪힌다. 그래서 평소 이동으로 보내는 `moveToStand`를 함께 둔다.
  - **연출용 이동 잠금**(`useCameraStore`의 `locks`): 스테이션 밖 장식도 이동을 잠글 수 있게 한다.
    영역 밖으로 걸어 나가 닫히는 경우엔 우클릭을 누른 채라, 잠그지 않으면 되돌려 보낸 목표점이
    매 프레임 커서로 덮어써진다. 개수로 세어 연출이 겹쳐도 각자 걸고 각자 푼다.
  - **잠금의 두 얼굴**: 입력을 받는 `World`는 스테이션·장식 잠금을 함께 보고(`isMovementBlocked`),
    목표점을 현재 위치로 스냅하는 `Character`는 **진입 잠금만** 본다. 장식 잠금까지 스냅에 걸면
    잠그는 순간 캐릭터가 굳어 지정한 자리로 걸어가지 못한다.
  - **등장 조건 공용화**(`MapDecorations.hooks`): `useAfterStation` — 그 스테이션을 열었다 닫은 뒤인지.
    아직 가보지 않은 곳으로 유도하는 장식은 앞 스테이션을 보고 나온 뒤에 나타나야 한다.
  - **신호등**(`scene/MapDecorations/TrafficLight/`): 종이 스티커를 눕히지 않고 세운다. 세운 종이는
    그 자체로 서 있는지 읽히지 않으므로 **바닥에 그림자를 따로 깐다** — 같은 그림을 검게 눌러 눕히고
    길이·방향·진하기를 조절한다. 판 안에 인쇄되는 그림자는 끈다. 판이 향하는 방향은 팔로우 오프셋에서
    구해 화면을 정면으로 본다(미니맵이 회전각을 유도하는 것과 같은 방식). 자리는 횡단보도 우상단
    꼭지점 기준 상대 좌표라 횡단보도를 옮기거나 키우면 따라온다.
  - **종이 스티커는 컴포넌트로만**(DECISIONS 025): 배럴에서 굽는 훅을 빼고, 재질(`color`·`opacity`·
    `depthWrite`)과 여러 장 늘어놓기(`count`·`gap`)를 `<PaperSticker>` props로 받는다. 훅을 꺼내
    판을 직접 만들던 `ExitSticker`·`SkillLevel`을 컴포넌트로 바꿨다.
  - **개발용 HUD**: 맵 장식 패널에 횡단보도(크기·좌표·회전·그리는 시간·다시 그리기)와
    신호등(세로크기·좌표·회전·그림자 방향/길이/진하기) 폴더 추가.
  - **남긴 한계**: 모서리 둥글기(`roundness`)는 값만 있고 눈에 띄는 차이를 만들지 못한다.
    지금은 각진 쪽으로 두고 쓴다.

- **2026-08-07**
  - **Phase 8 — `about-career` 정적 오브젝트와 활성화 전환** 구현. 페이지 내용(경력·교육·자격증 렌더링)과
    나가기는 아직 없다.
  - **영역**: 상단 중앙 (13.5, 25)을 앵커로 둔 사각형. 좌상단을 기준으로 두는 다른 스테이션과 달리
    횡단보도를 건너온 자리와 맞물리는 점을 기준으로 삼는다. 중심·좌상단은 거기서 계산한다.
  - **비활성 모습**: 오려 붙인 종이 두 장(교육·자격증)과 트로피 모델. 겹침 순서는 `renderOrder`로 못 박는다.
    반투명은 카메라 거리로 정렬돼, 아래쪽에 놓인 종이가 위로 올라오기 때문이다.
  - **트로피 바닥 그림자**(DECISIONS 027): 부품이 여럿인 모델이라 반투명하게 눕히면 겹친 곳만 진해진다.
    실루엣을 스텐실에 표시해 두고 그 자리만 한 겹 칠해 진하기를 고르게 하고, 알파로 섞어 모눈이 비치게 한다.
    실루엣은 모델을 옆으로 눕히지 않고 **위에서 눌러 눕히는 전단 변환**으로 만든다. 옆으로 눕히면
    깊이 방향으로 뻗은 손잡이가 몸체 뒤에 가려 사라진다.
  - **활성화 전환**(DECISIONS 026): 캐릭터가 **가장 가까운 영역 테두리**로 걸어간 뒤, 카메라 정면 회전과
    그림들의 로고 전환이 **동시에** 돈다. 영역 안에서 열면 걸을 곳이 없으므로 이동을 건너뛴다.
  - **로고 배치**: 영역을 셋으로 나눠 교육·수상내역·자격증 칸에 좌측 정렬한다. 배율이 아니라 **공통 높이**로
    맞춰 평소 크기가 제각각이어도 로고 줄이 흐트러지지 않는다. 트로피는 눕고 반 바퀴 단위로 더 돌며,
    누운 만큼 그림자가 걷힌다.
  - **제목**: 칸 로고 오른쪽에 손글씨로 붙는다. 간격은 로고 중심이 아니라 **잰 그림 가로** 기준이라
    그림마다 폭이 달라도 같은 간격으로 보인다.
  - **스테이션 자리 계약 확장**(DECISIONS 023 갱신): `stand`를 값뿐 아니라 함수로도 등록한다.
    Career는 캐릭터가 선 자리에 따라 테두리 위 지점이 달라지므로 부를 때마다 계산한다.
  - **`<PaperSticker>` 확장**(DECISIONS 025 갱신): `onMeasure`로 구운 그림의 월드 가로를 알린다.
    가로는 굽기 전에는 알 수 없어 왼쪽 정렬·제목 간격을 상수로 잡을 수 없다.
  - **정리**: 이동 경계 30 → 50(영역이 기존 범위 밖으로 나감), 그림자 스텐실을 위해 Canvas 스텐실 버퍼를 켠다.
  - **개발용 HUD**: Career 영역 패널(영역·트로피·교육·자격증·로고·제목) 신설.
  - **트러블슈팅**: 첫 화면에서 Intro 사진이 두 번 그려지던 문제를 hotfix로 처리(LEARNING 2026-08-07).

- **2026-08-09**
  - **Phase 8 — `about-career` 인터랙션 UI 완료.** 활성 화면의 세 칸을 Firestore 값으로 채우고 나가기를 붙인다.
  - **`about-award` 폐지**(DECISIONS 013 갱신): 칸 구성이 이미 교육·수상내역·자격증인데 `awards`는
    다른 스테이션에 매핑돼 있었다. Career가 `education`·`awards`·`spec`을 담고 `about-award`를 없앤다.
    스테이션은 8개에서 7개(About 3)로 줄고, 경력(`experiences`)은 자리가 없어 이번 범위에서 뺀다.
  - **세 칸을 한 형태로**(`CareerColumns`, DECISIONS 028): 컬렉션마다 필드가 달라 그리기 전에
    `{ 제목, 본문, 좌/우 한 줄 }`로 정규화한다. 그리는 쪽은 한 종류만 다루고, 칸이 늘어도 변환만 더한다.
    정렬은 전부 최신순이고 기간은 `2023.01 ~ 2023.12`(진행 중이면 `~ 현재`) 꼴로 찍는다.
  - **좌/우 배치**: 제목 아래 한 줄에 왼쪽 기간 · 오른쪽 기관명을 두고, 오른쪽은 칸 오른쪽 끝에 건다.
    왼쪽 글이 길어져도 오른쪽 줄이 가지런하다. 수상은 그 위에 설명이 더 들어간다.
  - **인용 막대**: 수상 설명은 줄마다 기호를 붙이지 않고 Intro와 같이 왼쪽에 세로 막대를 세운다.
    여러 줄이어도 한 인용 블록 안에서 줄만 나뉘고, 막대 높이는 troika가 잰 접힌 본문 높이를 따라간다.
  - **밑선 맞추기**(LEARNING 2026-08-09): 크기가 다른 글자를 한 줄에 두면 상단을 맞출 때 밑선이 어긋난다.
    제목을 `anchorY="top-baseline"`으로 두고 베이스라인을 기준점으로 삼는다.
  - **항목 높이**: 본문이 몇 줄로 접힐지 미리 알 수 없어 재서 알려주는 높이로 다음 자리를 잡는다.
    본문이 없는 교육·자격증은 글자 크기로 바로 계산돼 기다리지 않는다. Firestore와 측정은
    `useStationGate`로 잡아 뒀다가 다 되면 한 번에 보여준다.
  - **칸 구분선**: 칸이 맞닿는 경계에 세로선을 세운다. 자리는 칸 왼쪽 테두리에서 나오고 개수는 칸 수에서
    유도하며, 세로 전체가 아니라 위아래를 들여 짧게 긋는다.
  - **영역 여백**(DECISIONS 029): 영역 테두리에서 내용까지 들이는 여백을 `StationAreaPadding`으로 공용화한다.
    로고·제목·목록·구분선이 그 안쪽을 기준으로 놓이고, **나가기 같은 UI는 따르지 않는다** — 구석에 붙는
    조작 요소라 여백을 넓혀도 그 자리에 남아야 한다.
  - **나가기 스티커 계약 정리**(DECISIONS 022 갱신): 종이에 눕는 각을 `ExitSticker`가 갖는다.
    쓰는 쪽마다 눕히는 그룹을 두면 공용으로 만든 뜻이 없다. 스테이션은 자리와 크기만 넘긴다.
  - **글씨**: 항목 제목도 본문체(Pretendard)로 두고 크기로만 구분한다. 손글씨는 칸 이름까지만 쓴다.
    날짜·기관명은 곁들이는 값이라 연한 회색으로 물러나 있다.
  - **개발용 HUD**: Career 패널에 목록·구분선·나가기 폴더를 더하고, 영역 폴더에 상하·좌우 여백을 넣는다.
  - **확정값**: 영역 14 × 8, 여백 (0.2, 0.3), 나가기 크기 0.6·여백 0.2.

- **2026-08-10**
  - **에셋 출처 표기**(`ui/Credits`, DECISIONS 030): 가져다 쓴 3D 모델의 출처를 화면에 둔다. 크레파스 버튼
    왼쪽 CC 로고 버튼을 누르면 모달이 열리고, 왼쪽 목록에서 고른 에셋을 오른쪽 캔버스에 띄워 실물과 출처를
    함께 본다. 목록은 `content/credits.ts`이고 에셋을 가져오면 여기 한 줄 넣는다.
  - **미리보기 정규화**: 모델마다 크기·원점이 제각각이라 경계 상자를 재 가장 긴 변을 1로 맞추고 중심을
    원점에 둔다. 카메라를 고정한 채 어떤 모델도 화면에 담기고, 거리 제한값이 모델과 무관하게 같은 뜻을 갖는다.
    캐시에 든 모델을 그대로 넣으면 스테이션에 서 있던 것이 옮겨져 오므로 복제해서 쓴다.
  - **회전·확대**: 직접 구현하지 않고 OrbitControls가 카메라를 구면 좌표로 다루는 방식을 그대로 쓴다.
    자동 회전에 드래그 회전·휠 확대를 열되 거리를 제한하고 팬은 끈다 — 기준점을 옮기면 모델을 화면 밖으로
    밀어낼 수 있어 거리 제한이 무의미해진다. 목록 항목을 누르면 처음 자세로 되돌리며, 보던 것을 다시 눌러도
    되돌아간다.
  - **구석 버튼 공용화**(`ui/CornerButton`): 크레파스 버튼이 갖고 있던 스타일을 옮겨 오른쪽 아래에 나란히
    서는 버튼을 하나로 둔다. 자리는 `$slot`(오른쪽에서 몇 번째)으로 계산해 크기·간격을 바꿔도 겹치지 않는다.
  - **라이선스 파일**: 폰트 두 종의 OFL 전문을 폰트 파일과 같은 자리에 두고(`public/fonts/*/OFL.txt`),
    레포 루트에 `LICENSE`를 둔다. 코드만 MIT이고 그림·사진·3D 에셋과 이력서 내용은 제외한다.
  - **Pretendard 서브셋 이름**: 예약된 폰트 이름을 알면서 그대로 둔다. 서브셋은 OFL이 말하는 개작본이라
    규정대로면 이름을 바꾸거나 허락을 받아야 하지만, 이 파일은 3D 글자를 그리는 데만 쓰여 이름이 폰트 목록에
    오르지 않는다. 해석에 기댄 판단이라는 것을 DECISIONS 030에 함께 적어 둔다.
  - **트러블슈팅**: 미리보기를 끌다 배경에서 손을 떼면 모달이 닫히던 문제 = 브라우저가 누른 곳과 뗀 곳의
    **공통 조상**에 클릭을 주는 것이 원인. 안쪽에서 전파를 막아도 그 이벤트는 배경에 바로 꽂히므로,
    누른 곳이 배경이었는지 기억해 배경에서 시작한 클릭만 닫기로 친다.

- **2026-08-13**
  - **Phase 8 — `projects` 스테이션 신설.** 정적 오브젝트와 비활성 → 활성 전환까지. 실내 화면은 아직 없다.
  - **구역 하나로 묶음**(DECISIONS 031): `project-1·2·3` 대신 `projects` 하나를 둔다. 건물이 곧 구역이고
    전시대는 그 안의 내용이라, 프로젝트가 늘어도 배치 데이터를 손대지 않는다.
  - **문 앞만 근접 판정**: 건물은 덩치가 커서 전체를 범위로 두면 뒤편에서도 문이 눌린다. 들어가는 곳은
    문 하나이므로 **문 앞 사각형**만 판정 범위로 두고, 클릭 대상도 문에 붙인 판 하나다.
  - **활성 전환**: 캐릭터가 문 앞으로 걷는 것과 카메라가 문 정면으로 도는 것이 함께 돌고, 그다음 문이 열리고,
    이어서 캐릭터가 안으로 걸어 들어가며 카메라가 문 쪽으로 당겨진다. 나올 때는 문이 먼저 닫히고 카메라가 복귀한다.
    다른 스테이션이 바닥을 수직으로 내려다보는 것과 달리 **세워 둔 문을 눈높이에서 정면으로** 본다.
  - **문 여닫기**: `door` 머티리얼이 지붕 판까지 함께 써서 통째로 떼면 엉뚱한 것이 딸려 온다. 덩어리로 갈라
    **세워 둔 판**만 문으로 보고 앞쪽 한 장을 뽑아 경첩에 매단다. 문짝 범위 안에 든 조각(창)은 머티리얼이
    달라도 함께 떼어 낸다 — 그러지 않으면 창만 제자리에 남는다.
  - **문간 빛**: 광원을 두지 않는다. 빛은 표면만 밝히고 공기 중에 보이지 않으며, 안쪽 벽을 밝히면 가리려던
    내부가 드러나고, 가림을 계산하지 않아 벽을 뚫고 바깥까지 밝힌다. 대신 면 넷(문쪽 면·카펫·빛 바닥·뒷면)을 둔다.
    캐릭터를 감추는 코드도 없다 — 빛 면이 캐릭터보다 앞에 있어 깊이 검사만으로 가려진다.
  - **막는 사각형**(`useBlockersStore`): 건물을 통과하지 못하게 한다. 무엇이 막는지는 막는 쪽이 올리고
    `Character`는 목록만 보고 밀려난다. 연출 이동 중에는 건너뛴다 — 건물 안으로 들여보내는 것도 연출이다.
  - **잰 값에 배치를 붙임**(DECISIONS 033): 문 자리를 상수로 박아 두니 건물 크기를 바꿀 때마다 클릭 판·표시·
    근접 구역·서는 자리가 모두 어긋났다. 문을 재서 `useProjectsDoorStore`에 올리고, 손으로 두는 값은
    **모델 좌표**로 둬 건물 배율이 곱해지게 했다.
  - **자동차 주행 연출**(맵 장식): Career를 닫으면 차가 나타나고, 눌러 타면 프로젝트 구역까지 데려다준다.
    받아 온 모델은 노드도 메시도 하나지만 지오메트리는 덩어리 11개라, **바닥에 닿은 원기둥** 넷을 바퀴로 보고
    따로 뽑아 굴린다. 굴림 배수를 둔 이유는 바퀴가 30° 간격 12각형이라 그대로 굴리면 프레임마다 한 칸씩
    건너뛰어 거꾸로 도는 것처럼 보이기 때문이다.
  - **공용으로 뺀 것**: 덩어리 가르기(`lib/geometryIslands` — 자동차 바퀴와 건물 문이 나눠 쓴다),
    `usePointerCursor`·`ClickMarker`를 `src/scene/` 밑으로(스테이션 밖에서도 쓴다).
  - **구조 정리**: `ProjectsArea/`와 그 안의 `ProjectsBuilding/` 두 겹을 한 겹으로 합쳤다. 건물이 곧 구역이라
    바깥 한 겹이 하는 일이 없었다.
  - **트러블슈팅**: 걸어 들어간 뒤 캐릭터가 도로 밀려나던 문제 = 도착해 연출 이동이 끝나는 순간 밀어내기가
    다시 걸린 탓. 활성인 동안에는 발자국을 빼 둔다.
  - **트러블슈팅**: 확대해도 문이 아니라 건물 가운데로 당겨지던 문제 = 카메라가 보는 점이 `보는 높이`(3.5)라
    문 중심(0.92)보다 한참 위였던 탓. **문 중심을 화면 중앙에 두는 자세**를 하나 더 두고 배율과 함께 섞는다.

- **2026-08-20**
  - **Phase 8 — 프로젝트 구역 실내(로비)** 구현. 문으로 걸어 들어간 뒤의 공간을 만들고 맵과 이었다.
    전시대와 체험 데모는 아직 없다.
  - **실내를 별도 라우트로**(DECISIONS 034): 로비는 `/projects`이고 **Canvas도 따로**다. 세계가 통째로
    갈리므로 이동 상태·판정을 맵과 나눠 갖는다. 맵이 직교인 것과 달리 **원근 카메라**를 쓴다 —
    실내는 깊이가 보여야 방으로 읽힌다.
  - **장면 전환**(`ui/SceneTransition`): 원형으로 조여드는 덮개가 라우트보다 위(`Root`)에 있다.
    **다 덮인 뒤에 옮기고, 도착한 쪽이 준비를 알려야 연다**(`scene/SceneArrival`). 그래서 모델을
    받는 동안에도 화면이 끊기지 않는다. 조여드는 중심은 씬이 매 프레임 채우므로 **문 쪽으로** 모인다.
  - **맵과 잇기**: 문이 열려 캐릭터가 걸어 들어가기 시작할 때 전환도 함께 시작한다 — 도착을 기다리면
    걷기가 끝난 뒤 덮개가 따로 도는 두 동작이 된다. 돌아오면 캐릭터를 문 앞에 세우고 스테이션을
    즉시 닫는다(`closeImmediately`). 브라우저 뒤로가기는 주소만 되돌리므로 맵이 뜰 때도 같은 정리를 한다.
  - **열어본 기록을 스토어로**(`useStationStore.visited`): 라우트가 갈리면 컴포넌트에 든 기록이
    사라져, 로비에서 돌아왔을 때 맵 장식이 없어지고 클릭 표시가 되살아난다.
  - **첫 카메라 자리를 캐릭터 기준으로**(`AERIAL_OFFSET`): 상수 좌표로 박아 두면 로비에서 문 앞으로
    돌아왔을 때 카메라만 시작점 근처에 놓여 오프셋이 틀어진다. `SceneGate`도 첫 화면일 때만 그린다.
  - **실내 룩은 환경광이 만든다**(DECISIONS 035): 금속은 확산광이 없어 **환경 반사가 없으면 검게 남고**,
    대리석 광택도 반사에서 나온다. 블렌더 World는 glb에 안 담기므로 `RoomEnvironment`를 구워 쓴다.
    "어둡게"와 "광택 유지"가 한 손잡이에 묶이던 것은 재질에 **자기 환경맵**을 물려 갈랐다.
  - **벽등**: glb의 광원은 닿는 거리가 안 담겨 무한대로 들어와 여기서 자른다. 벽에 파묻혀 있어
    방 안쪽으로 떼어 놓는다 — 빛 웅덩이가 얇은 띠로 나오던 진짜 원인이다. 그림자는 한 번 굽고 얼리되,
    켜는 등 개수가 곧 셰이더 비용이라 입구 쪽 둘만 켠다.
  - **실내 이동**: 조작은 맵과 같은 우클릭 홀드다. 다른 점은 **높이** — 옮긴 자리에서 밟을 바닥을
    찾아 y를 맞추므로 경사 슬래브로 만든 계단을 미끄러지듯 오르내린다. 밟을 것이 없으면 걸음을
    물리므로 맵처럼 이동 경계를 따로 두지 않고, 남쪽만 시작 자리에서 잘라 막는다.
  - **계단에서는 단 가운데에 선다**: 판정용 계단은 단이 없는 경사 슬래브라, 눈에 보이는 계단 메시를
    재서 단 중앙 z를 얻고 목표점을 거기로 맞춘다.
  - **막는 것을 면으로**(DECISIONS 036): 경계 상자 하나로 재면 **기울여 놓은 난간·벽의 기울기가 사라져**
    계단을 침범한다. 삼각형마다 월드로 옮겨 면을 만들고 볼록체로 판정한다. 감는 방향은 믿지 않고
    안쪽 점으로 바로잡는다. 모델에 없는 면(난간 아래)은 있는 것을 **아래로 늘려** 메운다.
  - **실내 카메라**(DECISIONS 037): 방이 좁아 캐릭터를 그대로 따라가면 화면이 계속 흔들린다.
    제한을 건 기준점을 따라가고, 깊이는 계단 절반을 넘어선 만큼에만 비율이 걸린다.
    트리거를 열면 진행도 하나로 평소 자세와 포커스 자세를 섞어 구도가 어긋나지 않는다.
  - **모델을 코드에서 손보기**(DECISIONS 032 갱신): 방을 앞뒤로 늘려 쓰는데 둥근 것은 눌리므로,
    책·연단은 **감싸는 그룹에 역배율을 걸어** 빼낸다. 메시의 로컬 배율을 건드리면 돌아 있는 조각이
    찌그러진다. 액자에는 사진을 걸고, **테두리 재질은 glb의 JSON 청크를 직접 고쳤다** —
    직접 만든 모델이라 원본을 보존할 이유(CC-BY 표기)가 없다.
  - **트리거**: 연단 위 책과 북쪽 통로에 누를 판을 세운다. 잰 값으로 판을 직접 세우고,
    얹기는 R3F 이벤트로, 누르기는 캔버스 `mousedown`으로 나눈다 — 우클릭 홀드 중에는 `pointerdown`이
    발생하지 않는다. 아직 열어본 적 없는 트리거 위에만 맵과 같은 클릭 표시를 띄운다.
    보는 동안에는 커서·클릭·이동을 모두 막는다.
  - **책에 글 쓰기**(DECISIONS 038): 페이지가 곡면이라 3D 글씨로는 못 얹는다. 캔버스에 그려 텍스처로
    올리되, 한 메시에 옆면이 함께 있어 **면 방향과 책등 띠로 갈라 글 쓰는 면에만** 준다.
    좌우는 재질을 공유하므로 메시 이름으로 나눈다. 왼쪽은 손글씨 제목과 환영글, 오른쪽은
    Firestore `projects`를 `order`순으로 제목·요약.
  - **글꼴은 기다렸다 그린다**: 준비 전에 그리면 조용히 기본 글꼴로 나온다. 범위별 서브셋이라
    그릴 글자를 함께 넘겨야 조각을 받는다. 판을 들고 있다가 글꼴·데이터가 오면 다시 그린다.
  - **좌상단 뒤로 가기**(`ui/BackButton`): 앱 공용이다. 자리와 화살표는 버튼이 갖고 **색과 동작은
    쓰는 쪽이** 정한다 — 화면마다 돌아갈 곳과 배경이 다르다. 로비에서는 책이 열려 있으면 책을 닫고
    아니면 맵으로 나가며, ESC도 같은 길을 탄다.
  - **에셋**(DECISIONS 039): 로비 모델만 Draco로 압축돼 있어 디코더를 self-host한다. drei 기본값이
    외부 CDN이고 디코더는 모듈 하나를 공유하므로 **기본값 자체를** 바꾼다. 1.8MB라 앱이 뜰 때 받으면
    첫 화면이 늦어, **문 앞에 다가서면 미리 받는다.**
  - **개발용**: 콜라이더를 역할별 단색으로 그려 보는 표시, leva 패널을 `DevPanel`로 떼어 라우트마다
    하나씩(맵·로비), 로비 패널(카메라·트리거·판정).
  - **트러블슈팅**: 로비에서 우클릭 홀드가 안 먹고 클릭만 되던 문제(LEARNING 2026-08-20).
  - **트러블슈팅**: 기울어진 난간을 그냥 통과하던 문제(LEARNING 2026-08-20).
  - **트러블슈팅**: 빛 웅덩이가 원이 아니라 얇은 띠로 나오던 문제(LEARNING 2026-08-20).
  - **남긴 한계**
    - 그림자를 켠 앞쪽 등 옆에 네모난 얼룩이 남는다. `bias`·`mapSize`·`cameraNear`·필터를 다 만져도
      안 잡힌다. 계속 안 잡히면 그림자를 접는 것도 방법이다.
    - 1층 바닥에 계단·난간 그림자가 안 나온다. 벽에는 나온다. 원인 미확정.
    - 반구광·노출은 재질별로 나눌 수 없어 바닥도 함께 밝아진다.
    - 캐릭터는 그림자를 드리우지 않는다. 그림자를 얼려 뒀기 때문이다.
    - 액자가 벽면과 나란하지 않고 평면상 기울어 있어, 원본 배율로 되돌리면 한쪽이 벽을 파고든다.
      모델에서 액자를 벽면에 맞춘 뒤에 배율에서 빼낸다.
    - 책 글씨의 캔버스 가로세로 비는 눈으로 맞춰야 한다. 페이지가 정사각이 아니다.

## 다음 할 일

1. 통로 너머 전시 공간 — 북쪽 통로 트리거를 눌러 들어가는 다음 공간
2. 전시대를 Firestore `projects` 개수대로 생성 (고정 개수가 아니다)
3. 전시대별 "체험하기" 데모
4. `guestbook` 스테이션
5. 경력(`experiences`)을 어느 스테이션에 담을지 결정 — 지금은 매핑된 곳이 없다
